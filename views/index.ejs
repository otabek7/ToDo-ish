<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ToDo App</title>
  </head>

  <body>
    <div class="container">
      <h2>A Simple ToDo List App</h2>
      <form action="/addtask" method="POST">
        <input type="text" name="newtask" placeholder="E.g. Walk my fish" />
        <input type="submit" value="Add Task" />

        <h2>Added Tasks:</h2>
        <% for( var i = 0; i < task.length; i++){ %>
        <li>
          <input type="checkbox" name="inputted" value="<%= i %>" /> <%= task[i]
          %>
          <!--TODO: change the "value" to the time it was entered at?-->
        </li>
        <% } %>
        <button formaction="/removetask" type="submit">Remove</button>
      </form>
      <h2>Completed task</h2>
      <% for(var i = 0; i < complete.length; i++){ %>
      <li><input type="checkbox" checked /><%= complete[i] %></li>
      <% } %>
    </div>
  </body>
</html>
